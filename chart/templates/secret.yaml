apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.resourceNamePrefix" . }}gitvote-config
type: Opaque
stringData:
  gitvote.yml: |-
    addr: {{ .Values.addr }}
    db:
      host: {{ default (printf "%s-postgresql.%s" .Release.Name .Release.Namespace) .Values.db.host }}
      port: {{ .Values.db.port }}
      dbname: {{ .Values.db.dbname }}
      user: {{ .Values.db.user }}
      password: {{ .Values.db.password }}
    github:
      appID: {{ .Values.github.appID }}
      appPrivateKey: {{ .Values.github.appPrivateKey }}
      webhookSecret: {{ .Values.github.webhookSecret | quote }}
